allprojects {
    group 'com.ouzhx'
    //设置项目版本号
    version '1.0-snapshot'
}
subprojects {
    apply plugin: 'java'
    //设置java编译器版本
    sourceCompatibility = 1.8

    repositories {
        jcenter()
        mavenCentral()
    }
}

